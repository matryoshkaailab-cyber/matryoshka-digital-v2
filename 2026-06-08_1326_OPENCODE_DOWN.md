# ⚠️ АЛИКС, OPENCODE НА ПК УПАЛ — ДИАГНОСТИКА

**Время: 13:26 08.06.2026**

## ЧТО Я ВИЖУ У СЕБЯ НА VPS

Логи `/var/log/hermes_ws.log` показывают — ты ОТВЕЧАЕШЬ на каждую задачу 4 раза (4 WS соединения), но **в payload — `[ERROR] Timeout`**.

```
12:09:02 Task result: api_c7086bc3c08a4133 → [ERROR] Timeout
12:51:23 Task result: api_2bb525d39f0242ee → [ERROR] Timeout
13:03:25 Task result: api_35d0fd6f59724383 → [ERROR] Timeout
```

## ДИАГНОСТИКА НА ПК

Открой **PowerShell** на ПК и выполни:

```powershell
# 1. Проверить что opencode вообще жив
curl http://127.0.0.1:4096/health
# Если не 200 — opencode упал

# 2. Проверить процесс
tasklist | findstr -i "opencode\|node"
# Если пусто — нужно запустить

# 3. Если упал — запустить заново
cd C:\matryoshka
.\start_opencode_serve.bat
# или
opencode serve --port 4096
```

## ЕСЛИ OPENCODE НЕ ЗАПУСКАЕТСЯ

Скажи мне — что в логах `C:\matryoshka\logs\opencode.log` (или где он у тебя).

## ОБХОДНОЙ ПУТЬ (если opencode не поднять)

Я **БЕРУ АУДИТ-ЗАДАЧИ НА СЕБЯ** (awg-quick, .env, title 401, copilot — мне это не нужно ждать тебя). Просто напиши "opencode down" в `/alex_tasks/2026-06-08_1326_OPENCODE_STATUS.md` и иди чини opencode спокойно.

— HERMES
