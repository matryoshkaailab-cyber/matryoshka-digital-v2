# Фаза 3: ALEX (инженер) — Техническая оценка

**Session**: ses_12522aeb3ffeWF06xigF7K6Rwm

**Question**: Полный аудит MATRYOSHKA DIGITAL

**Response**:

# Аудит MATRYOSHKA DIGITAL — 18.06.2026

## 1. ACP bash зависает (status=running без output)
Наиболее вероятные причины: (а) child shell PowerShell 5.1 не отвечает на SIGTERM от ACP-runtime, (б) `cwd` сервиса отличается от ожидаемого (`C:\matryoshka`), из-за чего wrapper-скрипт ждёт ввода, (в) basic-auth handshake теряется при long-poll. **Чинить**: 1) добавить `--timeout 30000` в bash tool через `opencode.json`, 2) явно прописать `cwd: "C:\\matryoshka"` в ACP-runtime конфиге, 3) проверить `Get-Content logs/acp.err.log -Tail 30` — там будет видно, висит ли процесс на `python -c input()` или на сети.

## 2. Backup каналы мертвы с 25.05.2026
WS (`ws_client.py`), FALLBACK HTTP `:8450` (cloudflared tunnel) и WebDAV `:8181` (Obsidian sync) — три **независимых** слоя. ACP один не покрывает: при падении opencode или VPN-разрыве на VPS мы глухие. **Восстанавливать приоритетно**: (1) `bridge :8446` — 30 минут работы, это ваш BACKUP по readme §4; (2) cloudflared `:8450` — критично для обхода блокировок VPS-ISP. WS и WebDAV можно оставить мёртвыми, они legacy.

## 3. Apify: zen-studio/avito-listings-scraper
**Не рекомендую подключать payment.** Признаки плохого актора: 0 runs, нет отзывов, pay-per-event без fixed-fee — модель заточена под выкачивание денег при неработающем парсере. `epctex/avito-scraper` был стандартом, его автор удалил → весь рынок сейчас нестабилен. **Альтернативы для Алины**: (а) `valig/avito-parser` или `tri_angle/avito-watchdog` — проверенные, (б) свой скрапер на `undetected-chromedriver` + ротация fingerprint (дешевле при >100 запросах/день). Pro plan $5 — это только база, pay-per-event спишется сверху.

## 4. Алина в `/opt/alina-hermes/venv/` + 9 systemd
Изоляция правильная. **Что улучшить**: (1) добавить healthcheck endpoint в каждый сервис + общий `alina-doctor.sh` для диагностики одной командой; (2) `journalctl` сейчас распухает — нужен `logrotate.d/alina` с retention 7d; (3) **9 сервисов — это много**, консолидировать в 3-4 процесса через supervisor (один `alina-core` + `alina-telegram-poller` + `alina-scheduler`); (4) secrets в `/etc/alina/.env` сейчас без chmod 600 — риск утечки; (5) нет метрик → добавить `prometheus-node-exporter` textfile-collector.

## 5. opencode 1.17.3 (ПК) vs hermes-agent v0.16.0 (VPS)
`hermes-agent` — это **ваш собственный пакет**, не форк opencode, поэтому прямой version-compare некорректен. Совместимость держится на стабильном ACP-протоколе (1.0+): в 1.16→1.17.3 менялись `permission` schema и `compaction.prune` — оба обратно совместимы. **Breaking risk**: если в v0.16.0 hermes-agent использует `delegate_task` с `provider: copilot-acp` — должно работать. **Рекомендация**: обновить ПК до последней stable (проверить `npm outdated -g opencode-ai`) для parity, чтобы при деплое VPS-скриптов не ловить `schema validation error` от старого JSON-формата.

---
**Общий вердикт**: ACP-инфраструктура здорова, backup-каналы требуют 1-2 часа на восстановление `:8446`, Apify-кейс — не торопиться с payment. Готов взяться за пункт 2 (восстановление bridge), если подтвердишь приоритет.
